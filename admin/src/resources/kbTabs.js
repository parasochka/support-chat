import { t } from '../i18n';

/** Shared tab strip between the KB topics list and the KB variables list. */
export const KB_TABS = [
  { path: '/kb', label: t('Topics & KB texts') },
  { path: '/kb_variables', label: t('Variables') },
];
