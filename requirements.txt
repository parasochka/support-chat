# Runtime deps. Upper bounds cap the next MAJOR so an unpinned rebuild months
# later can't silently pull a breaking major (e.g. openai 2.x, whose error
# classes / client surface openai_client.py constructs and matches on for the
# two-key failover + truncation self-heal would differ — and the failover tests
# are excluded from the runtime image, so nothing would catch it). Bump a ceiling
# deliberately after verifying the new major against the suite.
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
asyncpg==0.29.0
openai>=1.58.0,<2.0.0
httpx>=0.27.0,<1.0.0
python-multipart>=0.0.9,<1.0.0
Pillow>=10.3.0,<12.0.0
